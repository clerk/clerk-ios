name: Checks

on:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  checks:
    name: Shared checks
    permissions:
      actions: read
      checks: write
      contents: read
    uses: ./.github/workflows/shared-checks.yml
    with:
      run_integration_tests: true
    secrets: inherit
