name: Checks

on:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  checks:
    name: Shared checks
    permissions:
      contents: read
    uses: ./.github/workflows/shared-checks.yml
